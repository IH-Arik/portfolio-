# CORS configuration (JSON array format)
ALLOWED_ORIGINS=["http://localhost:3000"]

# Database location configuration (SQLite default)
DATABASE_URL=sqlite:///app/data/contact.db

# SMTP configuration for email notifications (SMTP forwarding)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=

# RAG Future expansion settings
USE_LLM_GENERATION=False
LLM_API_KEY=
